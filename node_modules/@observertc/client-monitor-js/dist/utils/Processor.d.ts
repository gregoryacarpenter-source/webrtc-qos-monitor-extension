export type Middleware<T> = (input: T, next: (input: T) => void) => void;
export declare class Processor<T> {
    private middlewares;
    private _finalCallback;
    addMiddleware(middleware: Middleware<T>): void;
    removeMiddleware(middleware: Middleware<T>): void;
    process(input: T): void;
    addFinalCallback(callback: (input: T) => void): void;
    private _next;
}
