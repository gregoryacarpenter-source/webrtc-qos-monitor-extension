import { ClientMonitor } from "..";
import { RtcStats } from "../schema/W3cStatsIdentifiers";
import { StatsCollector } from "./StatsCollector";
import { Logger } from "../utils/logger";
export declare class RtcPeerConnectionStatsCollector implements StatsCollector {
    readonly peerConnection: RTCPeerConnection;
    readonly clientMonitor: ClientMonitor;
    private readonly logger;
    lastStats: RtcStats[];
    get browser(): {
        name: "chrome" | "firefox" | "safari" | "edge" | "opera" | "unknown";
        version: string;
    } | undefined;
    constructor(peerConnection: RTCPeerConnection, clientMonitor: ClientMonitor, logger?: Logger);
    getStats(): Promise<RtcStats[]>;
    private _extend;
    private _firefoxTrackStats;
}
