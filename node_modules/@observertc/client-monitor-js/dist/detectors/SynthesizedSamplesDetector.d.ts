import { MediaPlayoutMonitor } from "../monitors/MediaPlayoutMonitor";
import { Detector } from "./Detector";
export declare class SynthesizedSamplesDetector implements Detector {
    readonly mediaPlayout: MediaPlayoutMonitor;
    readonly name = "synthesized-samples-detector";
    disabled: boolean;
    constructor(mediaPlayout: MediaPlayoutMonitor);
    private get peerConnection();
    private get config();
    update(): void;
}
