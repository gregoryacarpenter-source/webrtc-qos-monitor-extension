import { Detector } from "./Detector";
import { InboundTrackMonitor } from "../monitors/InboundTrackMonitor";
export type AudioDesyncIssuePayload = {
    peerConnectionId: string;
    trackId: string;
    dCorrectedSamples: number;
    fractionalCorrection: number;
    durationInMs?: number;
};
export declare class AudioDesyncDetector implements Detector {
    readonly trackMonitor: InboundTrackMonitor;
    static readonly ISSUE_TYPE = "audio-desync";
    readonly name = "audio-desync-detector";
    disabled: boolean;
    private readonly issueKey;
    constructor(trackMonitor: InboundTrackMonitor);
    private _startedDesyncAt?;
    private _prevCorrectedSamples;
    private get config();
    private get peerConnection();
    update(): void;
    private _raise;
    private _resolve;
}
