import { InboundTrackMonitor } from "../monitors/InboundTrackMonitor";
import { Detector } from "./Detector";
export type DryInboundTrackIssuePayload = {
    trackId: string;
    duration: number;
    durationInMs?: number;
};
export declare class DryInboundTrackDetector implements Detector {
    readonly trackMonitor: InboundTrackMonitor;
    static readonly ISSUE_TYPE = "dry-inbound-track";
    readonly name = "dry-inbound-track-detector";
    disabled: boolean;
    private readonly issueKey;
    constructor(trackMonitor: InboundTrackMonitor);
    private _evented;
    private _startedDryAt?;
    private get peerConnection();
    private get config();
    private _activatedAt?;
    update(): void;
    private _raise;
    private _resolve;
}
