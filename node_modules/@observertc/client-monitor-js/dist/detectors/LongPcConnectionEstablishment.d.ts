import { Detector } from "./Detector";
import { PeerConnectionMonitor } from "../monitors/PeerConnectionMonitor";
export declare class LongPcConnectionEstablishmentDetector implements Detector {
    readonly peerConnection: PeerConnectionMonitor;
    readonly name = "long-pc-connection-establishment-detector";
    disabled: boolean;
    private get config();
    private _evented;
    constructor(peerConnection: PeerConnectionMonitor);
    update(): void;
}
