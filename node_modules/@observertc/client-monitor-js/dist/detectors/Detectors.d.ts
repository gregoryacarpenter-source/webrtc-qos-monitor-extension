import { Detector } from "./Detector";
export declare class Detectors implements Iterable<Detector> {
    private _detectors;
    private readonly logger;
    constructor(...detectors: Detector[]);
    add(detector: Detector): void;
    remove(detector: Detector): void;
    clear(): void;
    get size(): number;
    get listOfNames(): string[];
    [Symbol.iterator](): IterableIterator<Detector>;
    has(name: string): boolean;
    getByName<T extends Detector = Detector>(name: string): T | undefined;
    find(predicate: (detector: Detector) => boolean): Detector | undefined;
    filter(predicate: (detector: Detector) => boolean): Detector[];
    disable(name: string): boolean;
    enable(name: string): boolean;
    disableAll(): void;
    enableAll(): void;
    isEnabled(name: string): boolean;
    update(): void;
}
