import { PeerConnectionMonitor } from "../monitors/PeerConnectionMonitor";
import { Detector } from "./Detector";
export declare class IceTupleChangeDetector implements Detector {
    readonly pcMonitor: PeerConnectionMonitor;
    readonly name = "ice-tuple-change-detector";
    constructor(pcMonitor: PeerConnectionMonitor);
    readonly tuples: Set<string>;
    update(): void;
}
