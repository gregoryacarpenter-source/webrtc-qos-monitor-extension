import { InboundTrackMonitor } from "../monitors/InboundTrackMonitor";
import { Detector } from "./Detector";
export type PlayoutDiscrepancyIssuePayload = {
    trackId: string;
    frameSkew: number;
    ewmaFps: number;
    durationInMs?: number;
};
export declare class PlayoutDiscrepancyDetector implements Detector {
    readonly trackMonitor: InboundTrackMonitor;
    static readonly ISSUE_TYPE = "inbound-video-playout-discrepancy";
    readonly name = "playout-discrepancy-detector";
    disabled: boolean;
    private readonly issueKey;
    private _startedDiscrepancyAt?;
    constructor(trackMonitor: InboundTrackMonitor);
    private get peerConnection();
    private get config();
    active: boolean;
    update(): void;
    private _raise;
    private _resolve;
}
