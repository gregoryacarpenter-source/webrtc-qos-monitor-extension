import { Detector } from "./Detector";
import { OutboundTrackMonitor } from "../monitors/OutboundTrackMonitor";
export type SimulcastLayerState = {
    rid: string;
    ssrc: number;
    encodingIndex?: number;
    active: boolean;
    bitrate?: number;
    frameWidth?: number;
    frameHeight?: number;
    framesPerSecond?: number;
    scalabilityMode?: string;
};
export declare class SimulcastLayerDetector implements Detector {
    readonly trackMonitor: OutboundTrackMonitor;
    readonly name = "simulcast-layer-detector";
    disabled: boolean;
    private _previousActiveKeys?;
    constructor(trackMonitor: OutboundTrackMonitor);
    private get config();
    private get peerConnection();
    update(): void;
}
