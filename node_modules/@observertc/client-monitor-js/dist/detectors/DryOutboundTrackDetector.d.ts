import { OutboundTrackMonitor } from "../monitors/OutboundTrackMonitor";
import { Detector } from "./Detector";
export type DryOutboundTrackIssuePayload = {
    trackId: string;
    duration: number;
    durationInMs?: number;
};
export declare class DryOutboundTrackDetector implements Detector {
    readonly trackMonitor: OutboundTrackMonitor;
    static readonly ISSUE_TYPE = "dry-outbound-track";
    readonly name = "dry-outbound-track-detector";
    disabled: boolean;
    private readonly issueKey;
    constructor(trackMonitor: OutboundTrackMonitor);
    private _evented;
    private _startedDryAt?;
    private get peerConnection();
    private get config();
    private _activatedAt?;
    update(): void;
    private _raise;
    private _resolve;
}
