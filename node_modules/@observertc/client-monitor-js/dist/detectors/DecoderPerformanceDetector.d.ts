import { Detector } from "./Detector";
import { InboundTrackMonitor } from "../monitors/InboundTrackMonitor";
export type DecoderPerformanceIssuePayload = {
    peerConnectionId: string;
    trackId: string;
    decodeTimePerFrameInMs?: number;
    frameBudgetInMs?: number;
    dropRatio?: number;
    renderRatio?: number;
    framesReceived: number;
    decoderImplementation?: string;
    powerEfficientDecoder?: boolean;
    consecutiveTicks: number;
    durationInMs?: number;
};
export declare class DecoderPerformanceDetector implements Detector {
    readonly trackMonitor: InboundTrackMonitor;
    static readonly ISSUE_TYPE = "video-decoder-overloaded";
    readonly name = "decoder-performance-detector";
    disabled: boolean;
    private readonly issueKey;
    private _consecutiveTicks;
    private _alertOn;
    private _startedAt?;
    constructor(trackMonitor: InboundTrackMonitor);
    private get config();
    private get peerConnection();
    update(): void;
    private _clear;
}
