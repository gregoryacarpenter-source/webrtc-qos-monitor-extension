import * as W3CStats from '../schema/W3cStatsIdentifiers';
import { Logger } from '../utils/logger';
import { StatsAdapter } from './StatsAdapter';
export declare class StatsAdapters {
    private readonly logger;
    readonly adapters: Map<string, StatsAdapter>;
    constructor(logger?: Logger);
    add(adapter: StatsAdapter): void;
    remove(adapter: StatsAdapter | string): boolean;
    adapt(input: W3CStats.RtcStats[]): W3CStats.RtcStats[];
    postAdapt(input: W3CStats.RtcStats[]): W3CStats.RtcStats[];
}
