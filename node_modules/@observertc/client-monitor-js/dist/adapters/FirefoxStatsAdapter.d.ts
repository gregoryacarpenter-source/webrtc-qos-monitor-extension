import { RtcStats } from "../schema/W3cStatsIdentifiers";
import { StatsAdapter } from "./StatsAdapter";
export declare class FirefoxStatsAdapter implements StatsAdapter {
    readonly name = "firefoxStatsAdapter";
    private readonly _transportStats;
    private _selectedCandidatePair?;
    private _accumulatedAt?;
    adapt(stats: RtcStats[]): RtcStats[];
    private _addReconstructedTransport;
}
