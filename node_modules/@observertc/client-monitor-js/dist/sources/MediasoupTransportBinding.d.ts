import * as mediasoup from 'mediasoup-client';
import { PeerConnectionMonitor } from '../monitors/PeerConnectionMonitor';
export declare class MediasoupTransportBinding {
    readonly transport: mediasoup.types.Transport;
    readonly monitor: PeerConnectionMonitor;
    constructor(transport: mediasoup.types.Transport, monitor: PeerConnectionMonitor);
    unbind(): void;
    bind(): void;
    private _consumerPaused;
    private _consumerResumed;
    private _producerPaused;
    private _producerResumed;
    private _producerAdded;
    private _createProducerTrackWatcher;
    private _consumerAdded;
    private _dataConsumerAdded;
    private _dataProducerAdded;
    private _connectionStateChanged;
    private _iceGatheringStateChanged;
    private _fireEvent;
}
