import * as mediasoup from 'mediasoup-client';
import { Sources } from './Sources';
export declare class MediasoupDeviceBinding {
    readonly device: mediasoup.types.Device;
    readonly sources: Sources;
    constructor(device: mediasoup.types.Device, sources: Sources);
    bind(): void;
    unbind(): void;
    private _transportAdded;
}
