import { PeerConnectionTransportStats } from "../schema/ClientSample";
import { PeerConnectionMonitor } from "./PeerConnectionMonitor";
export declare class PeerConnectionTransportMonitor implements PeerConnectionTransportStats {
    private readonly _peerConnection;
    private _visited;
    timestamp: number;
    id: string;
    dataChannelsOpened?: number;
    dataChannelsClosed?: number;
    attachments?: Record<string, unknown> | undefined;
    appData?: Record<string, unknown> | undefined;
    constructor(_peerConnection: PeerConnectionMonitor, options: PeerConnectionTransportStats);
    get visited(): boolean;
    accept(stats: Omit<PeerConnectionTransportStats, 'appData'>): void;
    getPeerConnection(): PeerConnectionMonitor;
    createSample(): PeerConnectionTransportStats;
}
